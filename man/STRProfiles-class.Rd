% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{STRProfiles-class}
\alias{STRProfiles-class}
\title{STRProfiles: a set of short tandem repeat profiles}
\value{
An object of class \code{STRProfiles}.
}
\description{
An S4 container holding STR profiles for a set of samples, the markers they
were typed at, per-sample metadata, and a record of how the object was built.
}
\details{
Alleles are stored \strong{pre-split}: each column of the \code{alleles} slot is an
\link[IRanges:CharacterList]{IRanges::CharacterList} whose i-th element is the character vector of
unique alleles called for sample i at that marker. A sample that was not
typed at a marker has a zero-length entry.

This is the main structural departure from the \code{strprofiler} Python package,
which stores comma-joined strings and re-splits them on every single pairwise
comparison. Splitting once at ingest is what lets \code{\link[=scoreProfiles]{scoreProfiles()}} express
scoring as a handful of sparse matrix products.

Build one with \code{\link[=readSTRProfiles]{readSTRProfiles()}} (from files) or \code{\link[=STRProfiles]{STRProfiles()}} (from a
\code{data.frame} already in memory).
}
\section{Slots}{

\describe{
\item{\code{alleles}}{A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with one row per sample and one column
per marker. Every column is a \link[IRanges:CharacterList]{IRanges::CharacterList}.}

\item{\code{markerData}}{A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with one row per marker, row names
matching \code{colnames(alleles)}. Always carries a \code{class} column
(see \code{\link[=classifyMarkers]{classifyMarkers()}}) and an \code{nTyped} column.}

\item{\code{sampleData}}{A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with one row per sample, row names
matching \code{rownames(alleles)}. Holds non-marker columns such as \code{Center} and
\code{Passage}.}

\item{\code{provenance}}{A list recording the source files, ingest options,
timestamp, and package version.}
}}

\examples{
f <- system.file("extdata", "ExampleSTR_long.csv", package = "STRprofilerR")
p <- readSTRProfiles(f, sampleCol = "Sample Name")
p

}
\seealso{
\code{\link[=readSTRProfiles]{readSTRProfiles()}} to build one, \code{\link[=scoreProfiles]{scoreProfiles()}} to compare them.
}
\author{
Jared Andrews
}
