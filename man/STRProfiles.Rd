% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{STRProfiles}
\alias{STRProfiles}
\title{Build STRProfiles from a data.frame}
\usage{
STRProfiles(
  x,
  sampleCol = "Sample",
  pentaFix = TRUE,
  metadataCols = c("Center", "Passage"),
  extraAliases = NULL
)
}
\arguments{
\item{x}{A \code{data.frame} with a sample identifier column and one column per
marker.}

\item{sampleCol}{Name of the sample identifier column.}

\item{pentaFix}{Logical scalar. Harmonise the common Penta marker spellings
via \code{\link[=markerAliases]{markerAliases()}}.}

\item{metadataCols}{Character vector of non-marker column names to route into
\code{\link[=sampleData]{sampleData()}}. Columns not present in the input are ignored.}

\item{extraAliases}{Named character vector of additional marker aliases,
passed to \code{\link[=harmonizeMarkers]{harmonizeMarkers()}}.}
}
\value{
A \link{STRProfiles} object.
}
\description{
Constructs a \link{STRProfiles} object from a wide \code{data.frame} already in memory:
one row per sample, one column per marker, alleles as comma-separated
strings. This is the inverse of \code{as.data.frame()} on a \code{STRProfiles} object.
}
\examples{
df <- data.frame(
    Sample = c("Line1", "Line2"),
    AMEL = c("X,Y", "X"),
    vWA = c("16,18", "17"),
    "Penta D" = c("9,10", ""),
    check.names = FALSE
)
p <- STRProfiles(df)
p
alleles(p)[["PentaD"]]

# Round-trips through as.data.frame().
identical(as.data.frame(STRProfiles(as.data.frame(p))), as.data.frame(p))

}
\seealso{
\code{\link[=readSTRProfiles]{readSTRProfiles()}} to read from disk instead.
}
\author{
Jared Andrews
}
