% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markers.R
\name{markerAliases}
\alias{markerAliases}
\alias{harmonizeMarkers}
\title{Marker name harmonisation}
\usage{
markerAliases(reverse = FALSE, extra = NULL)

harmonizeMarkers(markers, reverse = FALSE, extra = NULL)
}
\arguments{
\item{reverse}{Logical scalar. If \code{TRUE}, return the inverse table mapping
compact spellings onto the spaced spellings expected by CLASTR.}

\item{extra}{Named character vector of additional aliases, where names are
the aliases to replace and values are the canonical names to use.}

\item{markers}{Character vector of marker names to harmonise.}
}
\value{
\code{markerAliases()} returns a named character vector whose names are aliases
and whose values are canonical marker names.

\code{harmonizeMarkers()} returns a character vector of the same length as
\code{markers}, with any recognised aliases replaced.
}
\description{
STR marker names are spelled inconsistently across instruments, vendors, and
databases. \code{markerAliases()} returns the alias table used to harmonise them,
and \code{harmonizeMarkers()} applies it to a vector of marker names.
}
\details{
The default table resolves the common Penta marker spellings (\verb{Penta C},
\code{Penta_C}, and so on) onto the compact forms (\code{PentaC}) used throughout this
package. This generalises the hard-coded \verb{_pentafix()} helper in the
\code{strprofiler} Python package: pass \code{extra} to harmonise any other marker
names your instrument produces.

The CLASTR API expects the \emph{spaced} spellings, so \code{reverse = TRUE} inverts
the table for outbound queries.
}
\examples{
markerAliases()

harmonizeMarkers(c("Penta D", "Penta_E", "vWA"))

# The CLASTR API wants the spaced spellings back.
harmonizeMarkers(c("PentaD", "PentaE"), reverse = TRUE)

# Harmonise a vendor-specific spelling too.
harmonizeMarkers("AMELOGENIN", extra = c(AMELOGENIN = "AMEL"))

}
\author{
Jared Andrews
}
