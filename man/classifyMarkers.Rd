% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markers.R
\name{classifyMarkers}
\alias{classifyMarkers}
\title{Classify STR markers}
\usage{
classifyMarkers(markers)
}
\arguments{
\item{markers}{Character vector of marker names.}
}
\value{
A character vector the same length as \code{markers}, with values
\code{"amelogenin"}, \code{"y-linked"}, or \code{"autosomal"}.
}
\description{
Tags each marker as amelogenin, Y-linked, or autosomal. The classification is
stored in the \code{class} column of \code{\link[=markerData]{markerData()}} and is what
\code{\link[=scoreProfiles]{scoreProfiles()}} uses to decide which markers to drop from scoring.
}
\details{
Y-linked markers are recognised by a \code{DYS} prefix (for example \code{DYS391}).
Everything that is neither amelogenin nor Y-linked is reported as
\code{"autosomal"}; the classification is a naming heuristic, not a lookup against
a curated marker panel, so unusual marker names may need correcting by hand
via \code{markerData(x)$class <- ...}.
}
\examples{
classifyMarkers(c("AMEL", "DYS391", "vWA", "PentaD"))

}
\author{
Jared Andrews
}
