% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarizeMatches}
\alias{summarizeMatches}
\title{Summarise pairwise scores into one row per query}
\usage{
summarizeMatches(
  scores,
  profiles = NULL,
  tanThreshold = 80,
  masQThreshold = 80,
  masRThreshold = 80,
  threeAlleleThreshold = 3,
  includeAlleles = TRUE
)
}
\arguments{
\item{scores}{A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} of pairwise scores from
\code{\link[=scoreProfiles]{scoreProfiles()}}.}

\item{profiles}{Optional \link{STRProfiles} of the query profiles. When supplied,
a \code{mixed} column and the query's allele columns are added.}

\item{tanThreshold}{Minimum Tanabe score to report in \code{tanabeMatches}.}

\item{masQThreshold}{Minimum Masters (query) score to report in
\code{mastersQueryMatches}.}

\item{masRThreshold}{Minimum Masters (reference) score to report in
\code{mastersRefMatches}.}

\item{threeAlleleThreshold}{Passed to \code{\link[=flagMixedSamples]{flagMixedSamples()}}. Ignored when
\code{profiles} is \code{NULL}.}

\item{includeAlleles}{Logical scalar. Append the query's allele columns.
Ignored when \code{profiles} is \code{NULL}.}
}
\value{
A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with one row per query sample and columns
\code{Sample}, \code{mixed} (optional), \code{topHit}, \code{nextBest}, \code{tanabeMatches},
\code{mastersQueryMatches}, \code{mastersRefMatches}, followed by the allele columns.
}
\description{
Condenses the pair-level output of \code{\link[=scoreProfiles]{scoreProfiles()}} into a per-query
overview: the best hits, every hit passing each score threshold, and an
optional mixing flag and copy of the query's alleles.
}
\details{
Hits are reported as \code{"name: score"}, joined by \code{"; "}, with scores shown to
two decimal places. Each of the three match columns is ordered by its own
score, descending. \code{topHit} and \code{nextBest} use the Tanabe score.

If \code{scores} was filtered (via the \code{minScore} argument of \code{\link[=scoreProfiles]{scoreProfiles()}}),
the summary reflects only the pairs that survived.
\subsection{Divergences from the Python package}{

\code{strprofiler} reads the top two hits positionally out of a table whose first
row is the query itself, reporting an empty string when a query has fewer than
two comparisons (through 0.4.2 it raised an \code{IndexError}). Here a query with no
comparisons gets \code{NA} for both, and one with a single comparison gets \code{NA} for
\code{nextBest}.

\code{strprofiler} orders all three match columns by Tanabe score, so the Masters
columns come out in an order unrelated to their own values. Each column is
sorted on its own score here.
}
}
\examples{
q <- readSTRProfiles(
    system.file("extdata", "ExampleSTR_long.csv", package = "STRprofilerR"),
    sampleCol = "Sample Name"
)
ref <- readSTRProfiles(
    system.file("extdata", "ExampleSTR_database.csv", package = "STRprofilerR"),
    sampleCol = "Sample Name"
)

scores <- scoreProfiles(q, ref)
summarizeMatches(scores, q)

# Scores only, without the mixing flag or alleles.
summarizeMatches(scores)

}
\seealso{
\code{\link[=scoreProfiles]{scoreProfiles()}} to produce \code{scores}, \code{\link[=compareProfiles]{compareProfiles()}} to do
both in one call.
}
\author{
Jared Andrews
}
