% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{readSTRProfiles}
\alias{readSTRProfiles}
\title{Read STR profiles from file}
\usage{
readSTRProfiles(
  files,
  sampleCol = "Sample",
  markerCol = "Marker",
  sampleMap = NULL,
  pentaFix = TRUE,
  metadataCols = c("Center", "Passage"),
  format = c("auto", "wide", "long"),
  extraAliases = NULL
)
}
\arguments{
\item{files}{Character vector of paths. Supported extensions are \code{csv},
\code{tsv}, \code{txt} (tab-separated), and \code{xlsx} (first sheet; needs \code{readxl}).}

\item{sampleCol}{Name of the sample identifier column.}

\item{markerCol}{Name of the marker identifier column. Used only for wide
files.}

\item{sampleMap}{Optional sample renaming table: either a path to a
headerless two-column CSV, or a \code{data.frame}/\code{matrix} with at least two
columns. The first column holds names as they appear in the input, the
second the names to use instead.}

\item{pentaFix}{Logical scalar. Harmonise the common Penta marker spellings
via \code{\link[=markerAliases]{markerAliases()}}.}

\item{metadataCols}{Character vector of non-marker column names to route into
\code{\link[=sampleData]{sampleData()}}. Columns not present in the input are ignored.}

\item{format}{One of \code{"auto"}, \code{"wide"}, or \code{"long"}. Applied to every file.}

\item{extraAliases}{Named character vector of additional marker aliases,
passed to \code{\link[=harmonizeMarkers]{harmonizeMarkers()}}.}
}
\value{
A \link{STRProfiles} object.
}
\description{
Reads one or more STR profile files, in either wide or long layout, into a
single \link{STRProfiles} object.
}
\details{
\subsection{Layouts}{

\strong{Long} files carry one row per sample, with every column other than the
sample column (and any \code{metadataCols}) treated as a marker:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Sample,  D1S1656, DYS391, AMEL
Line1,   "12,14", 12,     X
}\if{html}{\out{</div>}}

\strong{Wide} files carry one row per sample/marker pair, with the alleles spread
across several columns. Any column whose name contains \code{Allele} is collected;
size and height columns are ignored:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Sample, Marker,  Allele 1, Size 1, Allele 2
Line1,  D3S1358, 16,       128.29, 18
}\if{html}{\out{</div>}}

With \code{format = "auto"} a file is read as wide when it has a \code{markerCol}
column \emph{and} at least one column containing \code{Allele}, and long otherwise.
}

\subsection{Combining files}{

Files may mix layouts and formats. Markers are unioned across files, with
samples missing a marker recorded as untyped rather than as an empty allele.
Sample names must be unique across all files.
}

\subsection{Divergences from the Python package}{

\code{metadataCols} are held in \code{\link[=sampleData]{sampleData()}} rather than being treated as
markers. In \code{strprofiler} these columns flow into the scoring routine, so two
samples sharing a \code{Center} of \code{"JAX"} score as sharing a marker.

Row order follows the input files. \code{strprofiler} returns wide-format samples
in sorted order because it groups with \code{pandas}.

All columns are read as text, so alleles are never coerced to numbers and
back. This removes a class of bug that \code{strprofiler} patched twice (alleles
ending in zero being truncated, for example \code{10} becoming \code{1}).
}
}
\examples{
long <- system.file("extdata", "ExampleSTR_long.csv", package = "STRprofilerR")
p <- readSTRProfiles(long, sampleCol = "Sample Name")
p

# Penta spellings are harmonised by default.
markers(p)
markers(readSTRProfiles(long, sampleCol = "Sample Name", pentaFix = FALSE))

# A database file with Center and Passage metadata.
db <- system.file("extdata", "main_database.csv", package = "STRprofilerR")
ref <- readSTRProfiles(db)
sampleData(ref)[1:3, ]

# Rename samples on the way in.
smap <- system.file("extdata", "SampleMap_exp.csv", package = "STRprofilerR")
xlsx <- system.file("extdata", "ExampleSTR.xlsx", package = "STRprofilerR")
if (requireNamespace("readxl", quietly = TRUE)) {
    rownames(readSTRProfiles(xlsx, sampleCol = "Sample Name", sampleMap = smap))
}

}
\seealso{
\code{\link[=STRProfiles]{STRProfiles()}} to build one from an in-memory \code{data.frame},
\code{\link[=scoreProfiles]{scoreProfiles()}} to compare the result.
}
\author{
Jared Andrews
}
