% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-STRProfiles.R
\name{STRProfiles-methods}
\alias{STRProfiles-methods}
\alias{[,STRProfiles-method}
\alias{dim,STRProfiles-method}
\alias{dimnames,STRProfiles-method}
\alias{[,STRProfiles,ANY,ANY,ANY-method}
\alias{$,STRProfiles-method}
\alias{show,STRProfiles-method}
\alias{c,STRProfiles-method}
\alias{as.data.frame,STRProfiles-method}
\title{Dimensions, subsetting, and coercion for STRProfiles}
\usage{
\S4method{dim}{STRProfiles}(x)

\S4method{dimnames}{STRProfiles}(x)

\S4method{[}{STRProfiles,ANY,ANY,ANY}(x, i, j, ..., drop = FALSE)

\S4method{$}{STRProfiles}(x, name)

\S4method{show}{STRProfiles}(object)

\S4method{c}{STRProfiles}(x, ..., recursive = FALSE)

\S4method{as.data.frame}{STRProfiles}(x, row.names = NULL, optional = FALSE, ..., sampleCol = "Sample")
}
\arguments{
\item{x}{A \link{STRProfiles} object.}

\item{i}{Sample index: names, positions, or a logical vector.}

\item{j}{Marker index: names, positions, or a logical vector.}

\item{drop}{Ignored; present for compatibility with the \code{[} generic.}

\item{name}{A marker or sample annotation name.}

\item{object}{A \link{STRProfiles} object.}

\item{recursive}{Ignored; present for compatibility with the \code{c()} generic.}

\item{row.names, optional, ...}{Passed through to \code{as.data.frame()}.}

\item{sampleCol}{Name to give the sample identifier column.}
}
\value{
\code{dim()} returns an integer vector of length two; \code{dimnames()} a list of two
character vectors. \code{[} and \code{c()} return a \link{STRProfiles} object.
\code{as.data.frame()} returns a \code{data.frame}. \code{$} returns the named column of
\code{\link[=sampleData]{sampleData()}}.
}
\description{
Standard R idioms for inspecting and reshaping a \link{STRProfiles} object.
}
\details{
A \code{STRProfiles} object is samples-by-markers, so \code{dim()} reports
\code{c(n_samples, n_markers)} and \code{x[i, j]} subsets samples by \code{i} and markers by
\code{j}, both of which accept names, indices, or logical vectors.

\code{as.data.frame()} flattens the object back to the wide layout
\code{\link[=readSTRProfiles]{readSTRProfiles()}} accepts, with alleles collapsed to comma-separated
strings, so profiles round-trip through disk without loss.
}
\examples{
f <- system.file("extdata", "ExampleSTR_database.csv", package = "STRprofilerR")
p <- readSTRProfiles(f, sampleCol = "Sample Name")

dim(p)
colnames(p)

# Subset to three samples and drop amelogenin.
sub <- p[1:3, markerData(p)$class != "amelogenin"]
dim(sub)

as.data.frame(sub)

}
\author{
Jared Andrews
}
