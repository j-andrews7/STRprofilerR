% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clastr.R
\name{clastrQuery}
\alias{clastrQuery}
\title{Query the Cellosaurus knowledge base via CLASTR}
\usage{
clastrQuery(
  x,
  algorithm = c("tanabe", "mastersQuery", "mastersRef"),
  scoringMode = c("nonEmpty", "query", "reference"),
  scoreFilter = 80,
  minMarkers = 8,
  maxResults = 200,
  includeAmelogenin = FALSE,
  url = "https://www.cellosaurus.org/str-search/api/query/"
)
}
\arguments{
\item{x}{A \link{STRProfiles} object, or a named character vector giving one
profile's markers and comma-separated alleles.}

\item{algorithm}{Similarity score CLASTR should rank by.}

\item{scoringMode}{How CLASTR handles markers missing from one side:
\code{"nonEmpty"} uses markers present in both, \code{"query"} all query markers,
\code{"reference"} all reference markers.}

\item{scoreFilter}{Minimum score for a match to be returned.}

\item{minMarkers}{Minimum number of shared markers for a match to be
returned.}

\item{maxResults}{Maximum number of matches to return per query.}

\item{includeAmelogenin}{Logical scalar. Include amelogenin in CLASTR's
scoring.}

\item{url}{API endpoint. Exposed for testing.}
}
\value{
A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} with one row per returned profile and
columns \code{query}, \code{accession}, \code{name}, \code{species}, \code{score}, \code{accessionLink},
\code{problem}, followed by one column per marker. Zero rows if nothing matched.
}
\description{
Searches the human cell line profiles in
\href{https://www.cellosaurus.org}{Cellosaurus} for matches to one or more STR
profiles, using the
\href{https://www.cellosaurus.org/str-search/help.html}{CLASTR REST API}.
}
\details{
Each profile is submitted separately and the JSON responses are parsed into
one tidy table, with a \code{query} column naming the profile each row answers.
A result carrying two profiles (CLASTR returns at most two) is reported as
two rows, with \code{" (Best)"} and \code{" (Worst)"} appended to the accession, as the
Python package does.

Marker names are converted to CLASTR's spellings on the way out and back to
this package's on the way in, so \code{PentaD} round-trips correctly. Markers
CLASTR does not recognise are reported with a warning and ignored by the
search; check ahead of time with \code{\link[=validateClastrMarkers]{validateClastrMarkers()}}.

This function needs network access, and \strong{is for research use only}.
\subsection{Divergences from the Python package}{

The response is parsed directly from the nested JSON rather than through a
flatten-and-pivot chain over generated column names. \code{algorithm = 3} is sent
for Masters (reference) in batch mode, as \code{strprofiler} does from 0.5.0;
through 0.4.2 it sent \code{2}, which silently runs a Masters (query) search.
}
}
\examples{
\dontshow{if (interactive() && requireNamespace("curl", quietly = TRUE) && curl::has_internet()) withAutoprint(\{ # examplesIf}
profile <- c(
    Amelogenin = "X", CSF1PO = "13,14", D5S818 = "13", D7S820 = "8,9",
    D13S317 = "12", FGA = "24", TH01 = "8", TPOX = "11", vWA = "16"
)

hits <- clastrQuery(profile, scoreFilter = 80)
as.data.frame(hits)[, c("accession", "name", "score")]
\dontshow{\}) # examplesIf}
}
\references{
Bairoch A (2018). The Cellosaurus, a cell-line knowledge resource.
\emph{Journal of Biomolecular Techniques} 29(2):25-38. \doi{10.7171/jbt.18-2902-002}

Robin T, Capes-Davis A, Bairoch A (2020). CLASTR: The Cellosaurus STR
similarity search tool. \emph{International Journal of Cancer} 146(5):1299-1306.
\doi{10.1002/ijc.32639}
}
\seealso{
\code{\link[=clastrBatchQuery]{clastrBatchQuery()}} for CLASTR's own multi-sheet XLSX output,
\code{\link[=compareProfiles]{compareProfiles()}} to compare against a local database instead.
}
\author{
Jared Andrews
}
