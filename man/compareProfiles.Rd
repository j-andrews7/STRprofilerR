% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compareProfiles}
\alias{compareProfiles}
\title{Compare STR profiles end to end}
\usage{
compareProfiles(
  query,
  reference = NULL,
  useAmel = FALSE,
  excludeMarkers = NULL,
  tanThreshold = 80,
  masQThreshold = 80,
  masRThreshold = 80,
  threeAlleleThreshold = 3,
  minScore = NULL,
  minScoreType = c("tanabe", "mastersQuery", "mastersRef"),
  chunkSize = 2000L
)
}
\arguments{
\item{query}{A \link{STRProfiles} object holding the profiles to score.}

\item{reference}{A \link{STRProfiles} object to score against. If \code{NULL}, \code{query}
is compared against itself and self-pairs are dropped.}

\item{useAmel}{Logical scalar. Include amelogenin markers in scoring.}

\item{excludeMarkers}{Character vector of additional marker names to leave
out of scoring.}

\item{tanThreshold}{Minimum Tanabe score to report in \code{tanabeMatches}.}

\item{masQThreshold}{Minimum Masters (query) score to report in
\code{mastersQueryMatches}.}

\item{masRThreshold}{Minimum Masters (reference) score to report in
\code{mastersRefMatches}.}

\item{threeAlleleThreshold}{Passed to \code{\link[=flagMixedSamples]{flagMixedSamples()}}. Ignored when
\code{profiles} is \code{NULL}.}

\item{minScore}{Optional numeric scalar. Drop pairs scoring below this on
\code{minScoreType}. Applied per block, so it also bounds memory.}

\item{minScoreType}{Which score \code{minScore} applies to.}

\item{chunkSize}{Number of query profiles to score per block.}
}
\value{
A \link{STRComparison} object.
}
\description{
Scores profiles, flags potential mixing, and summarises the hits in one call.
This is the R equivalent of the Python package's \verb{strprofiler compare}
command, minus the file writing, which \code{\link[=writeSTRResults]{writeSTRResults()}} handles.
}
\details{
With \code{reference} supplied, each query is compared against the reference set
only, never against the other queries. This is the "batch against database"
mode. With \code{reference = NULL}, queries are compared against each other and
self-comparisons are dropped.

Unlike \code{strprofiler}, this returns an object rather than writing files, so
results can be inspected, filtered, or plotted before anything is committed
to disk. Pass the result to \code{\link[=writeSTRResults]{writeSTRResults()}} to produce the same file set
the Python CLI does.
}
\examples{
q <- readSTRProfiles(
    system.file("extdata", "ExampleSTR_long.csv", package = "STRprofilerR"),
    sampleCol = "Sample Name"
)
ref <- readSTRProfiles(
    system.file("extdata", "ExampleSTR_database.csv", package = "STRprofilerR"),
    sampleCol = "Sample Name"
)

cmp <- compareProfiles(q, ref)
cmp
summary(cmp)

# All-against-all within a single set.
compareProfiles(ref)

}
\seealso{
\code{\link[=scoreProfiles]{scoreProfiles()}} and \code{\link[=summarizeMatches]{summarizeMatches()}} for the individual steps,
\code{\link[=writeSTRResults]{writeSTRResults()}} to write the output files.
}
\author{
Jared Andrews
}
