% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{strHTMLTable}
\alias{strHTMLTable}
\title{Write a data frame as a standalone HTML table}
\usage{
strHTMLTable(x, file, title = "STRprofiler Results")
}
\arguments{
\item{x}{A \code{data.frame}.}

\item{file}{Path to write to.}

\item{title}{Page title.}
}
\value{
Invisibly, the path written to.
}
\description{
Writes a table to a self-contained HTML file, interactive where the optional
packages allow and static otherwise.
}
\details{
When \code{DT}, \code{htmlwidgets}, and \code{pandoc} are all available the table is written
as a \code{DT} widget: sortable, searchable, and paged. Otherwise a plain HTML
table with embedded styling is written. Both forms are self-contained and
render offline.
}
\examples{
f <- file.path(tempdir(), "table.html")
strHTMLTable(head(iris), f)
file.exists(f)

}
\seealso{
\code{\link[=writeSTRResults]{writeSTRResults()}}, which calls this for the summary table.
}
\author{
Jared Andrews
}
