% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{scoreQuery}
\alias{scoreQuery}
\title{Score a single profile against a reference set}
\usage{
scoreQuery(
  x,
  reference,
  sample = "Query",
  useAmel = FALSE,
  excludeMarkers = NULL,
  pentaFix = TRUE,
  keepCalls = c("X", "Y")
)
}
\arguments{
\item{x}{A named character vector or list mapping marker names to
comma-separated allele calls.}

\item{reference}{A \link{STRProfiles} object to score against.}

\item{sample}{Name to label the query with.}

\item{useAmel}{Logical scalar. Include amelogenin markers in scoring.}

\item{excludeMarkers}{Character vector of additional marker names to leave
out of scoring.}

\item{pentaFix}{Logical scalar. Harmonise Penta marker spellings in \code{x}.}

\item{keepCalls}{Character vector of non-numeric calls that count as alleles,
passed to \code{\link[=cleanAlleles]{cleanAlleles()}} and honoured at amelogenin markers only. Defaults
to the sex markers \code{X} and \code{Y}. Every other non-numeric call is an uncallable
peak or free text, and the per-sample count discarded is recorded in
\code{\link[=sampleData]{sampleData()}} as \code{nDroppedCalls}.}
}
\value{
A \link[S4Vectors:DataFrame]{S4Vectors::DataFrame} as returned by \code{\link[=scoreProfiles]{scoreProfiles()}}.
}
\description{
A convenience wrapper around \code{\link[=scoreProfiles]{scoreProfiles()}} for scoring one hand-entered
profile, as an interactive session or a query form would.
}
\examples{
ref <- readSTRProfiles(
    system.file("extdata", "ExampleSTR_database.csv", package = "STRprofilerR"),
    sampleCol = "Sample Name"
)

scoreQuery(
    c(marker1 = "12,14", marker2 = "12", marker4 = "13", AMEL = "X"),
    ref
)

}
\seealso{
\code{\link[=scoreProfiles]{scoreProfiles()}}, which this calls.
}
\author{
Jared Andrews
}
